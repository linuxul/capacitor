<?xml version="1.0" encoding="UTF-8"?>
<lint>
    <issue id="GradleDependency" severity="ignore" />
    <issue id="AndroidGradlePluginVersion" severity="ignore" />
    <!-- core-ktx is intentionally not a dependency of the runtime -->
    <issue id="UseKtx" severity="ignore" />
    <!-- trim { it <= ' ' } is java.lang.String.trim(); Kotlin's trim() strips Unicode whitespace, so the lambda is intentional -->
    <issue id="TrimLambda" severity="ignore" />
</lint>
